Must have SDL libraries and make installed before using this:

	apt-get install libsdl1.2-dev libsdl-image1.2-dev automake

Then run "make" command

To use, ensure ADB connection has been established by running and ensuring device is recognized:

	adb devices

In order to run and display images on screen run and everything should be done for you:

	./sdl_test